version: 0.2

phases:
  build:
    commands:
      - echo entered the build phase
      - mvn clean package
    post_build:
      commands:
        - echo build completed successfully
artifacts:
  files:
    - target/spring-boot-rest-api-h2-0.0.1-SNAPSHOT.jar
  discard-paths: yes